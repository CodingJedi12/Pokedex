<!DOCTYPE html>
<html>
    <link rel="stylesheet" href="/style.css">
    <head>
    <meta charset="utf-8" />
    <title>Pokedex</title>
  </head>
  <body>
    <div id="header">
        <h1>Pokedex</h1>
    </div>
    <div id="items">
        <% for (i = 0; i < pokedex.length; i++) { %>
        <h2><%=pokedex[i].name%> | ID:<%=pokedex[i].id%></h2>
        <a href="/pokemon/<%=[i]%>"><img src="<%=pokedex[i].img%>"></a>
        <% } %>
    </div>
  </body>
</html>